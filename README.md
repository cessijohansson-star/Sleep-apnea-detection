This repository consist of two project works. The first presents sleep apnea detecting using ECG signal. The work includes a background of sleep apnea, evaluation of the raw signal, prerocessing, feature extraction, and application of machine learning methods, which are evaluated. In the second project (Natural language processing), text classification task is implemented and the results are evaluated. 
